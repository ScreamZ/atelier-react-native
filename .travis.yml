language: node_js

node_js:
  - node

branches:
  only:
    - master
    - /^deploy-.*$/

cache:
  yarn: true
  directories:
    - ~/.npm
    - .jest

before_script:
  - yarn install

script:
  - yarn test --ci
  - npx expo login -u $EXPO_CLI_USERNAME -p $EXPO_CLI_PASSWORD
  - npx expo publish --non-interactive
